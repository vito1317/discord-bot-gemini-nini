# 追加段落：讀連結

接在已發布的公告之後。兩則皆在 2000 字內，無表格語法。
主要用「開發者對一般使用者」的口吻（同 GUIDE）；若你發的是技術版公告，用下方的技術版。

---

<!-- ════════ 使用者版 ════════ -->

# 🔗 補充：她現在也能讀連結了

公告發完之後又加了一個 —— **直接貼網址給她，她會去把那一頁讀完再回你。**

```
奈奈 幫我看這篇 https://arxiv.org/abs/2604.27393
奈奈 這個 PR 在改什麼 https://github.com/...
```

不用特別下指令，貼上去就好。Discord 那種用 `<網址>` 包起來不跳預覽的寫法她也認得。

**幾個要注意的**

- **貼了連結就不會再去搜尋** —— 你已經指定要看哪一頁了，她就專心讀那頁，不會另外 Google 一堆不相干的東西回來
- **PDF 連結也可以** —— 包括整頁都是圖的掃描檔，她會自動改用看圖的方式讀
- **圖片連結**也能直接貼
- 一則訊息最多讀 **2 個連結**，單頁上限 10MB
- 只讀得到**文章正文**。導覽列、留言區、需要登入或滑到底才載入的內容抓不到
- 讀不到就會說讀不到（被擋、逾時、要驗證碼…），一樣不會瞎掰

## 🔒 為什麼有些連結她不讀

如果你貼的是 `127.0.0.1`、`192.168.x.x`、`localhost` 這類**內部網址**，她會拒絕。

這不是懶，是刻意擋的。奈奈跑在一台同時跑著很多內部服務的機器上，如果不擋，任何人只要貼個內網網址，就能叫她去讀那些服務再把內容念出來 —— 等於把她變成偵察內網的工具。

所以只要網址解析到內網位址，她一律不讀。連「公開網址轉址到內網」這種繞法也擋掉了（每一次轉址都會重新檢查）。

要她看內網的東西的話，直接把內容複製貼上給她就好 👍

---

<!-- ════════ 技術版 ════════ -->

# 🔗 補充：URL fetching（含 SSRF 防護）

公告後又補了一個功能：貼網址給奈奈，她會抓回來讀。

技術上這功能的重點不在抓網頁，**在擋內網**。它把使用者輸入變成伺服器主動發出的請求，是標準的 SSRF 面 —— 而這台機器上跑著 llama-server、gateway、語音服務、docker 裡的 DB 等一堆內部服務。沒防護的話貼 `http://127.0.0.1:10003/v1/models` 就能叫她幫忙偵察內網並念出結果。

**擋掉的手法（12 種實測全過）**
- localhost 各種寫法：`127.0.0.1`、`localhost`、`0.0.0.0`、`[::1]`
- 雲端 metadata：`169.254.169.254`
- 私有網段：`10/8`、`172.16/12`、`192.168/16`、IPv6 `fc00::/7`
- 十進位 IP 編碼：`http://2130706433/`
- DNS 指回內網：`127.0.0.1.nip.io`
- 非 HTTP scheme：`file://`、`gopher://`

**最容易漏掉的是轉址。** 公開網址可以 302 到內網，只驗證第一跳等於沒驗證。所以關掉 httpx 的自動轉址改成**手動逐跳驗證**，並用本地 302 伺服器實測確認第二跳被擋、收到 0 bytes。

另外 DNS 若解析出多筆 IP，**只要有任何一筆是內網就整個拒絕**，避免 round-robin 繞過。

**內容處理**
- HTML 走 `trafilatura` 抽正文 —— 整份 HTML 塞進 prompt 會被導覽列和 script 洗掉，而後端 `--parallel 4` 讓每個 slot 實際只有 32k context
- PDF 連結直接複用附件那套解析（含掃描檔轉圖走 vision）
- 圖片連結走 vision
- 上限：2 個連結／單頁 10MB／注入 8000 字

有貼連結就不再另外搜尋 —— 使用者已經指定目標頁了。
