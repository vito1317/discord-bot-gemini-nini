# 奈奈更新日誌 — 2026-08-14（發公告用）

**用途**：貼在公告／管理員頻道的更新日誌。接在 `GUIDE-2026-08-12.md` 之後，
**只寫這次新增的內容** —— 重點關注用戶、人工／AI 訊息審核、管理控制台。

可分則貼上 Discord，每則皆在 2000 字內。Discord 不支援表格，故全部以清單呈現。

**本篇涵蓋**
- 🔎 管理員可設定「重點關注用戶」
- 👤 人工審核：核准後才發佈
- 🤖 AI 審核：自動核准或拒絕
- 🎛️ `/focus panel` 管理控制台
- 📎 附件、待審佇列與重啟後的處理方式
- ⚠️ 為什麼訊息會先消失、為什麼核准後不是本人帳號發送

---

<!-- ════════ 第 1 則 ════════ -->

# 🔎 新功能：重點關注用戶

管理員現在可以把需要觀察的成員加入「重點關注名單」。

名單裡的成員在伺服器發言時，訊息會先被奈奈收回，通過審核後才正式出現在原頻道。

## 怎麼設定

先設定管理員收件的頻道：

`/focus channel channel:#管理員`

再把成員加入名單：

`/focus add member:@某人`

解除關注：

`/focus remove member:@某人`

查看目前名單：

`/focus list`

## 哪些訊息會被審核

- 名單成員在伺服器內發出的文字
- 圖片、文件等附件
- 即使發在審核頻道，也一樣會攔截
- 私訊不在伺服器管理範圍內，不會攔截
- 機器人自己的審核卡片與代發訊息不會再次觸發，所以不會無限循環

這個功能需要奈奈有「管理訊息」「讀取訊息歷史」「傳送訊息」「嵌入連結」及
「附加檔案」權限。少了「管理訊息」時，她會通知管理員，不會假裝已經攔截成功。

---

<!-- ════════ 第 2 則 ════════ -->

# 👤 人工審核：管理員決定要不要發

切換方式：

`/focus mode mode:人工審核`

名單成員發言後，`/focus channel` 設定的頻道會收到一張審核卡片，內容包含：

- 發言者
- 原本要發到哪個頻道
- 訊息內容
- 審核編號
- **✅ 核准發佈**與**✖️ 拒絕**按鈕

按 **核准發佈**，奈奈會把內容送回原頻道；按 **拒絕**，內容就不會公開。
只有管理員能操作這兩顆按鈕。

## 待審佇列

`/focus pending` 可以查看目前還沒處理的訊息。

待審內容和附件會存進本機佇列，所以奈奈重啟之後，原本審核卡片上的按鈕仍然能用，
不會因為重新上線就全部失效。

兩位管理員同時按核准時，也只會成功發佈一次，不會在原頻道重複出現兩份。

---

<!-- ════════ 第 3 則 ════════ -->

# 🤖 AI 審核：讓奈奈先判斷

切換方式：

`/focus mode mode:AI 審核`

AI 會判斷純文字訊息能不能公開：

- 一般聊天、合理抱怨、心理支持與善意討論 → **通過並自動發佈**
- 騷擾、仇恨、威脅、露骨色情、個資外洩、詐騙、惡意連結、洗版，或鼓勵自傷／犯罪 → **拒絕**
- 不會只因為一個人心情不好、正在抱怨或需要支持就拒絕

不論通過或拒絕，審核頻道都會留一份紀錄和 AI 判斷理由。

## 這些情況仍會轉人工

- 訊息有圖片或其他附件
- AI 暫時連不上
- AI 回傳的結果無法解析
- AI 核准後，原頻道暫時無法發送

附件不會只看檔名就放行。AI 目前不能可靠檢查所有檔案內容，所以一律交給管理員。

**AI 已經即時核准或拒絕的訊息會立刻結案，因此不會留在「待審訊息」數字裡。**
只有正在等管理員處理的項目才算待審。

---

<!-- ════════ 第 4 則 ════════ -->

# 🎛️ 管理控制台

輸入：

`/focus panel`

會開啟一個只有管理員自己看得到的控制台，顯示：

- 審核頻道
- 關注人數
- 待審訊息數
- 目前是人工還是 AI 審核

控制台上的按鈕可以：

- 👥 查看名單
- 📨 查看待審佇列
- ℹ️ 查看操作說明
- 👤 切換人工審核
- 🤖 切換 AI 審核

加入／移除成員和設定審核頻道，仍使用 `/focus add`、`/focus remove`、
`/focus channel`，避免在按鈕上選錯人或選錯頻道。

`/settings view` 也會顯示重點關注系統目前的設定。

---

<!-- ════════ 第 5 則 ════════ -->

# ⚠️ 為什麼訊息會先消失？

Discord 沒有提供「使用者按送出，但先藏起來等 Bot 審核」的功能。

所以實際流程是：

1. 使用者送出訊息
2. 奈奈立即刪除原訊息
3. 人工或 AI 進行審核
4. 通過後，奈奈把內容重新發到原頻道

如果不先刪除，等待審核的幾秒或幾分鐘內，內容其實早就被所有人看到了，便失去
「通過審核才能發佈」的意義。

## 為什麼核准後不是本人發送？

Discord 不允許 Bot 使用真人帳號發訊息，也不能替真人重建一則真正屬於他的訊息。
因此核准後會由奈奈代為發佈，並在訊息上標示原發言者與「經審核後發佈」。

可以改成 Webhook 顯示原本的暱稱和頭像，外觀看起來比較接近本人；但 Discord 仍會標示
那是 Webhook／機器人訊息，不可能完全冒充真人帳號。這是 Discord 的安全限制。

---

<!-- ════════ 第 6 則 ════════ -->

# 📋 本次新增的管理員指令

`/focus channel` — 設定接收審核卡片的頻道  
`/focus add` — 加入重點關注用戶  
`/focus remove` — 移除重點關注用戶  
`/focus mode` — 切換人工／AI 審核  
`/focus list` — 查看重點關注名單  
`/focus pending` — 查看待人工審核佇列  
`/focus panel` — 開啟互動式管理控制台

## 建議第一次設定順序

1. `/focus channel channel:#管理員`
2. `/focus mode mode:人工審核` 或 `AI 審核`
3. `/focus add member:@要關注的成員`
4. 用 `/focus panel` 確認頻道、人數和模式
5. 請該成員發一則測試訊息，確認奈奈有刪除、送審及重新發佈

## 🐛 回報

如果訊息沒有被攔截，請附上：

- 發言者是誰
- 在哪個頻道發送
- 大概時間
- 控制台顯示的審核模式
- 審核頻道有沒有收到錯誤通知或卡片

有這些資料，才能分辨是名單、權限、AI、資料庫或頻道設定哪一段出了問題。

