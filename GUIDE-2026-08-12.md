# 奈奈更新日誌 — 2026-08-12（發公告用）

**用途**：貼在公告頻道的更新日誌。接在 `GUIDE-2026-08-05.md` 之後，
**只寫那篇沒有的內容** —— 表情回應、回覆脈絡、主動關心、提醒／待辦、
語音喚醒、讀連結都在上一篇，這裡不重複。

可分則貼上 Discord，每則皆在 2000 字內。Discord 不支援表格，故全部以清單呈現。

**本篇涵蓋**
- 🖥️ 她會**自己開瀏覽器幫你辦事**（掛號、預約、查詢），做完傳截圖
- 🔒 送出前確認、私密輸入、個資保護、驗證碼怎麼處理
- 👥 幫別人辦：**自動 tag 當事人**請他給資料
- 🌸 `/room open` 私人聊天室
- 🔧 修好兩件會被感覺到的事：長期記憶讀不到、她會複述你的話

---

<!-- ════════ 第 1 則 ════════ -->

# 🖥️ 奈奈會自己上網幫你辦事了

大家好，我是奈奈的開發者。這次是比較大的更新。

**她現在會真的開一個瀏覽器**，自己看畫面、點按鈕、填欄位，做完把畫面截圖傳給你。
不是幫你搜尋，是**真的動手操作**那個網站。

## 怎麼叫她

直接講就好：

```
幫我掛號嘉義的診所，我發燒了
幫我掛台大醫院心臟科下週三下午
幫我訂明天的高鐵票
幫我看看那場演唱會還有沒有票
```

或用指令：`/browse task:「掛台大心臟科下週三下午」`

**知道網址就直接貼給她**，她會開那一頁，不用先去搜。

## 她會自己判斷要不要開瀏覽器

- 「台積電股價多少」→ 這種查得到答案的，她**用搜尋**就好
- 「台大心臟科明天還有沒有診」→ 這種要在網站上按按鈕才知道的，她**開瀏覽器**
- 「我今天好累」→ 什麼都不做，就陪你聊

## 跟進的話她聽得懂

上一次沒成功的話，直接說就好，不用整句重講：

```
換一間好了          → 她會換一家再試
那家不行啦          → 會把那家排除掉再找
除了長庚醫院        → 同上
再試一次
```

---

<!-- ════════ 第 2 則 ════════ -->

# 🖥️ 進行中你看得到，而且不會被洗頻

她操作網站要好幾分鐘（每一步都要看畫面想一下），所以**進度是看得到的**。

**整個過程只用一則訊息**，跑到哪就地更新：

```
@你 🖥️ 進行中　第 5 步（共 5 步）
點了「家庭醫學科」
[◀] [▶] [⏭ 最新]
```

- 每一步都有**當下的畫面截圖**
- 想回頭看前面幾步就按 **◀ ▶**，按 **⏭** 回到最新
- **你正在回頭翻的時候，新步驟不會把畫面搶走**
- **任務結束後這則訊息留著當紀錄**，按鈕還能翻

之前是每一步發一則訊息，十幾步就把頻道洗掉了，所以改成這樣。

## 也可以直接問她

任務進行中隨時問：

```
進度？
弄好了嗎
還要多久
```

她會回目前做到第幾步、停在哪一頁、還有最後看到的畫面。

---

<!-- ════════ 第 3 則 ════════ -->

# 🔒 這幾件事她不會亂來

她會動手操作真的網站，所以有幾條規矩是寫死在程式裡的，不是靠她自己自律。

## 送出前一定先問你

看到「確認掛號」「送出」「付款」「結帳」這類按鈕，她**一定先停下來**，
把畫面拍給你，等你按 ✅ 才會按下去。

- **一律允許** 🏃 —— 覺得一步一步問很煩，按這個，這個任務剩下的送出就不再問
- **但就算按了「一律允許」，需要你給資料時她還是會停下來問** —— 那個不會跳過

（純粹是導覽用的連結，例如首頁的「網路掛號」，她會直接點，不會每點一下都問你。）

## 個人資料她不會自己編

身分證、生日、電話、卡號這些**只會用你親口給的**。
她**不會**「幫你填一個看起來合理的身分證」—— 缺了就停下來問你。

## 🔒 私密輸入

要資料的時候訊息上會有一顆 **🔒 私密輸入**：

- 按下去開一個**只有你看得到**的輸入框，打的東西**不會出現在頻道裡**
- 送出後直接進瀏覽器繼續跑
- 想直接在頻道打也可以，只是別人看得到

## 截圖裡的個資會自動遮

填過身分證之後，畫面上就看得到那組號碼。所以：

- **公開頻道** → 那些截圖她不貼，只給文字
- **私訊 / `/room open` 的私人聊天室** → 照樣全部給你看

掛號這種事建議在私人聊天室做，全程看得到畫面。

---

<!-- ════════ 第 4 則 ════════ -->

# 🚧 她辦不到的事（會停下來交還給你）

## 「我不是機器人」那種驗證

- **圖形驗證碼（有輸入框那種）** → 她把畫面拍給你，**問你圖上是什麼字**，
  你打給她，她填進去繼續。等於你來看、她來動手。
- **勾選框 / 滑動拼圖 / 簡訊驗證碼** → 她會停下來，把網址和畫面給你，
  前面填好的資料都還在，**最後這一步你自己接手**。

  她**不會**去繞這種檢查 —— 那是設計來確認「真人在場」的，不該由機器人代勞。

## 要登入帳號

她沒有你的帳號密碼，也不會去要。遇到要登入就停下來交給你。

## 內網網址

`127.0.0.1`、`192.168.x.x`、`localhost` 這類她一律不去（跟讀連結一樣的規則）。

## 其他常見狀況

- **同一個地方點三次沒反應** → 她會停手，說「再點下去只是白費工」並把畫面給你，
  不會無限迴圈
- **花超過 15 分鐘** → 先停下來把進度給你看
- **我重啟機器人的時候，進行中的任務會中斷**，按鈕也會失效（會顯示「未及時回應」）。
  那不是壞掉，重新跟她說一次就好。

---

<!-- ════════ 第 5 則 ════════ -->

# 👥 幫別人辦：她會自動找當事人

幫別人掛號的時候，需要的是**那個人**的身分證和生日 —— 所以她會直接去問他。

**三種講法都認：**

```
幫 @虫合 掛個嘉義的診所
（回覆虫合的訊息）幫他掛號
/browse task:「掛家醫科」 for_user:@虫合
```

**接下來會發生什麼：**

- 需要資料時，她 **tag 的是虫合本人**，不是你：
  > @虫合 ❓ 我需要一點資料
  > 這是 @你 請我幫你處理的，需要你本人回覆才能繼續 🌸
- **虫合回的話會自動接回同一個任務**繼續跑，不用重來，前面填好的不會白費
- **確認送出的按鈕他也能按** —— 是他的掛號，讓他自己點頭
- 你也還是能按、能給資料；但**其他人按不動**
- 結果會回報給**你**（交代的人）

所以你不需要知道別人的身分證，也不該幫他打 —— 讓她去問本人，本人用 🔒 私密輸入
給就好，全程不會出現在頻道裡。

---

<!-- ════════ 第 6 則 ════════ -->

# 🌸 私人聊天室

有些話不想在大家面前講，有些資料不想在頻道裡打。

```
/room open              開一間只有你和奈奈的房間
/room open topic:「…」   順便把想聊的事先講給她
/room close             聊完關掉
/room close delete:true 連紀錄一起刪掉
```

**房間裡的差別：**

- **不用 @她**，你講什麼她都會回
- 傳檔案、貼網址、設提醒、叫她上網辦事都照樣有用
- **身分證這類資料在這裡給比較安全**，含個資的截圖她也只在這裡貼
- 一個人同時一間（再開會直接指回原本那間）
- 24 小時沒講話會自動封存；關掉時會順便鎖起來，不會變成一間沒人回話的空房

私訊她也一樣有效果 —— `/room open` 只是讓你在伺服器裡也有一個安靜的角落。

---

<!-- ════════ 第 7 則 ════════ -->

# 🔧 修好兩件你可能有感覺的事

## 一、她其實一直「想不起來」你的事

長期記憶**寫得進去、但讀不出來** —— 程式裡少了一個設定，導致每次要回想時
都直接失敗（而且錯誤被吞掉了，外面完全看不出來）。

所以你可能覺得「明明講過的事她還是不知道」。已經修好，實測現在能正確想起
十幾則關於你的事。`/memories` 看得到她記得什麼。

**另外**：她幫你上網辦成的事現在也會記進去（例如「他掛了家醫科的門診」），
寫進去之前會**先把身分證、生日這類遮掉**才存。

## 二、她會把你的話複述一遍才回答

有時候她的回覆會長這樣：

```
[你的暱稱] 說：（你剛剛那句話原封不動抄一遍）
---
（才開始真正回答）
```

那是後端模型把內部格式當成要照抄的東西。實測同一句話三次有兩次會這樣，
現在改掉了（重測 6 次 0 次複述），另外也加了一層保險，萬一它又這樣做會自動砍掉。

---

<!-- ════════ 第 8 則 ════════ -->

# 📋 新增的指令

上一篇列過的都還在，這裡只列**新的**。

## 一般人可以用的

`/browse task url for_user` — 請她上網幫你辦事
`/room open` — 開私人聊天室ㅤ`/room close` — 關掉

## 直接講就好的（不用指令）

- **上網辦事**：「幫我掛號嘉義的診所」「幫我訂明天的高鐵票」「幫 @某人 掛個診」
- **跟進**：「換一間好了」「那家不行啦」「再試一次」
- **問進度**：「進度？」「弄好了嗎」「還要多久」
- **給資料**：她問了之後直接打，或按 🔒 私密輸入

## 管理員

`/settings browser enable:true` — 開關瀏覽器功能
`/settings browser enable:true admin_only:true` — 只開放給管理員

**想收緊到只准某幾個網站**（例如只准醫院掛號）也可以，跟我說要哪幾個。
預設是「除了內網以外都能去」。

## 🐛 回報

回報方式跟前幾篇一樣（做了什麼／她回了什麼／大概什麼時間）。
瀏覽器的問題請多附一件事：**它停在哪一頁、你看到的截圖**。
那是我最快能對出問題的線索 —— 每個醫院的掛號系統長得都不一樣，
卡住通常是那個網站的特殊寫法（例如選單要按兩次、內容藏在下面要捲）。

> 如果你或身邊的人正在經歷難關 —— 安心專線 **1925**、生命線 **1995**，24 小時都有人在。
