# 奈奈更新日誌 — 2026-08-05（發公告用）

**用途**：貼在公告頻道的更新日誌。`GUIDE-2026-08-02.md` 已經發過了，
**所以這篇只寫那篇沒有的內容** —— 附件／圖片、上網查、進度提示、長期記憶、
語音基本用法（`/join`、`/leave`、打斷回報）都不重複。

可分則貼上 Discord，每則皆在 2000 字內。Discord 不支援表格，故全部以清單呈現。

**本篇涵蓋**
- v1 之後才做的：自動按表情、跟著別人按、回覆脈絡、主動關心、提醒／待辦、天氣、計算、語音喚醒
- v1 寫錯／已變更的兩處更正（關鍵字觸發、即時情緒安慰）
- 早就在跑但 v1 沒寫進說明的：危險訊息通報、新成員審核（**不是新功能**，是補文件）
- v1 指令表缺的部分（新指令、前綴指令、自然語言講法、管理員指令）

---

<!-- ════════ 第 1 則 ════════ -->

# 🌸 奈奈又更新了 ── 補充說明

大家好，我是奈奈的開發者。

上次那篇公告（檔案、圖片、上網查、記憶、語音）**內容都還有效**，這篇只講**之後又加的東西**，還有兩個要更正的地方。

**這次新增的**
- 🌸 她會**自己按表情**在你的訊息上（不用跟她講話也會）
- 🫱 看到**別人按了表情**，她也會跟著按
- ↩️ 你用**回覆**指著某則訊息問她，她**看得到那則訊息的內容**了
- 💛 她會**主動來找你**（有兩種，下面詳細講）
- ⏰ **提醒**和 📝 **待辦**，直接講話就能設
- 🌤️ 查天氣、🧮 精確計算
- 🎙️ 語音**喚醒模式**

## ⚠️ 上一篇有兩件事要更正

**一、「訊息裡提到奈奈她就會回」現在是關閉的。**

太容易誤觸 —— 大家在聊她的時候她會一直插話。現在**有效的搭話方式**是這四種：

- **@奈奈** + 你想說的話
- **回覆**她的訊息
- **私訊**她
- 在 **#奈奈** 專屬頻道 —— 那裡不用 @ 她，隨便講她都會回

要把關鍵字觸發開回來的話，管理員一行指令就能開，跟我說。

**二、「她會立刻跳出來安慰你」也關掉了。**

同樣是誤判太多，變成一直插話。**但這不影響**：危險訊息通報照樣運作，過幾小時的追蹤關心也照樣運作（第 6 則會講）。

---

<!-- ════════ 第 2 則 ════════ -->

# 🌸 她會自己按表情了

現在她會在頻道裡幫訊息按 emoji，**不用跟她講話也會**。

**重點是：不是每則都按。** 要不要按是她看過內容之後自己判斷的，不是隨機亂按。

**她會按的**
- 你在分享心情、成果、日常小事
- 你難過、好累、焦慮 → 會給 🥺 🫂 💛 這種
- 報喜、慶祝、生日 → 🎉 🥰 👏
- 你在道謝、幫了別人、鼓勵別人

**她不會按的**
- 平淡沒什麼情緒的日常對話（「今天午餐吃便當」）
- 事務性的往來（「好」「收到」「了解」）
- 貼連結、程式碼、指令
- 吵架、罵人、政治宗教爭論 —— 按表情會像在選邊站
- **提到想死、自傷的訊息一律不按** —— 那種訊息按個表情很輕率，她會用文字好好回你

**她也會用這個伺服器自己的貼圖**，不是只有通用 emoji。

## 🫱 別人按了，她會跟著按

看到有人在某則訊息上按表情，她會跟著按同一個 —— 就像在群裡跟著大家附和。

- 跟之前會**等一下**（約 1.5 秒），不會你按完她瞬間貼上來
- 同一則訊息她**最多跟 2 個**，不會一則有 10 種表情她全部跟一遍
- **別的伺服器的自訂表情她跟不了** —— 這是 Discord 的限制，機器人只能用自己伺服器有的表情，不是她挑食
- 自傷相關的訊息她**不會跟**，別人按了什麼都不跟

## 🚫 她不會一直刷

- **同一個人連發好幾則**，整串只會被按一次，不會每則都貼
- **同一個頻道**短時間內她按的則數有上限，不會整片都是她的表情
- **不會一直給你同一個表情** —— 她記得最近給過你什麼，會換一個；換不出更貼切的就不按

**還是覺得太吵？** 跟管理員說。「主動按」、「跟著別人按」、「頻率」是三個獨立開關，可以只關其中一個。

---

<!-- ════════ 第 3 則 ════════ -->

# ↩️ 她看得到你回覆的那則訊息

以前你用 Discord 的**回覆**指著某則訊息問她，她其實看不到那則在講什麼，只看到你那句「這什麼意思？」，然後就會反問你在說什麼。現在修好了。

```
（小美說：我這週每天加班到半夜，快撐不住了）
 └ 你回覆那則 → @奈奈 你覺得他該怎麼辦？
```

她會知道你在問小美那件事。

**她看得到的**
- 那則訊息的**文字**
- 那則訊息裡的**圖片**（最多 2 張）—— 所以別人貼圖、你回覆那張圖問「這是什麼」也可以了
- 卡片（embed）的標題和內容、附件檔名、貼圖名稱

**要注意的**

- 在一般頻道，光回覆別人的訊息她不會跳出來 —— 你還是要 **@她**。在 **#奈奈** 頻道不用。
- 回覆**很久以前**的訊息也認得。以前只認快取裡的，回覆她三天前說的話會被當成沒回覆，現在會去把那則抓回來。
- 引用的內容**不會一直留在她的記憶裡** —— 聊很多輪之後她只記得「你當時回覆了某句話」，不會記得全文。要她一直記著就把重點打進訊息裡。

---

<!-- ════════ 第 4 則 ════════ -->

# 💛 她會主動來找你

有兩種，來源完全不同。

## 一、你自己約的

直接跟她講：

```
每天晚上9點來關心我一下
每週三下午5點問我一下狀況
```

- 沒說「每天」的話**預設就是每天**（會說「來關心我」的人通常想要持續的）
- 到時間她**不會念稿** —— 她會依照記得你的事臨場想一句，所以每次講的不一樣
- 她**遲到超過 2 小時就跳過那一次** —— 半夜補一句「早安」很怪

## 二、她自己排的（不用你要求）

如果你講的話讓她覺得你狀況不太好，她會**過幾小時之後回頭關心你一次**。

刻意做成這樣而不是定時排程，因為定時就變成騷擾了。它要同時滿足**全部**條件才會送：

- 情緒強度到達門檻、而且看起來是需要支持的（不是單純抱怨）
- **至少 6 小時後**才送 —— 先讓你喘口氣，不會立刻黏上來
- 挑**你平常會上線的時段**（她會統計你的活躍時間；樣本不夠就用晚上 8–10 點）
- 只找**她真的認識的人**（長期記憶裡有你的事），不會對整個伺服器發訊息
- 同一個人**至少隔 3 天**才會再被關心一次
- 送出前再檢查一次：如果你**最近半小時還在線上講話**，就跳過 —— 人就在這裡，這時候「主動關心」是打擾

訊息會 **@你**，不然你不會看到。

## 不想被打擾就直接說

```
不要主動來找我        → 全部關掉
不用再定時關心我了     → 同上
你可以偶爾主動關心我   → 重新打開
```

「不要主動來找我」是**全部**關掉，包含你自己約的定時關心，以後也不會再自動排。她還是隨時在，你想聊照樣找她。

## 查目前狀態

`/checkin_status` —— 看她學到你的活躍時段、記得你幾件事、有沒有待送的關心，以及**目前不會送出的原因**（例如「距上次不足 3 天」）。只有你自己看得到。

> 🔧 管理員可以 `/checkin_status @某人` 查別人的。這個之前是壞的（選項型別寫錯，點下去只會顯示「該申請未受回應」），已經修好。

---

<!-- ════════ 第 5 則 ════════ -->

# ⏰ 提醒和 📝 待辦

不用學指令格式，直接講就好。

## 有時間的 → 提醒

```
30分鐘後提醒我倒垃圾
明天早上8點提醒我開會
每天晚上10點提醒我吃藥
每週一提醒我交週報
平日早上7點叫我起床
```

- 相對時間（30 分鐘後、3 小時後）她會自己換算
- 沒講幾點：早上算 09:00、中午 12:00、下午 15:00、晚上 20:00
- 重複支援**每天／每週X／平日**
- 一個人最多 **30 個**提醒
- 她如果離線導致遲到超過 3 分鐘，訊息裡會跟你說遲了多久

`/reminders` 看你設了什麼。要取消就說「取消倒垃圾那個提醒」（同名的不只一個時她會問你是哪一個）。

## 沒時間的 → 待辦

```
幫我記一下要買牛奶
記得要繳電費
我的待辦有哪些
買牛奶做完了
```

- 沒講時間她就當**待辦**，不會亂設一個時間來吵你
- 最多 **50 個**

`/todos` 看清單。

## 🌤️ 順便還有這兩個

```
台北明天會下雨嗎        → 查天氣
幫我算 1234 乘以 56     → 精確計算（不是叫模型硬算，那會算錯）
```

---

<!-- ════════ 第 6 則 ════════ -->

# 🎙️ 語音喚醒 ／ 🔗 讀連結

## 語音喚醒模式 `/voice_wake`

上一篇講的 `/join`、`/leave` 沒變，這次多一個開關：

- **關閉**（目前的預設）—— 直接說話她就回應，一對一聊天用這個比較順
- **開啟** —— 要先叫「**奈奈**」她才會回應，喚醒後 **60 秒**內可以連續對話。**人多的頻道建議開**，不然她會回應所有人的閒聊。
- 喚醒詞認得奈奈／奶奶／娜娜／耐耐／nana 這些聽起來像的

## 🔗 讀連結的限制（常被問，列一次）

直接貼網址給她，她會去把那一頁讀完再回你。

- 一則訊息最多 **2 個**連結，單頁上限 10MB
- **貼了連結就不會再去搜尋** —— 你已經指定要看哪一頁了
- PDF 連結、圖片連結都可以
- 只讀得到**正文**。導覽列、留言區、要登入或滑到底才載入的內容抓不到
- **內網網址她一律不讀**（`127.0.0.1`、`192.168.x.x`、`localhost`…）。這是刻意擋的：她跑在一台同時跑很多內部服務的機器上，不擋等於任何人都能叫她去偵察內網。要她看內網的東西就直接把內容貼給她。

---

<!-- ════════ 第 7 則 ════════ -->

# 📋 新增的指令

上一篇列過的（`/chat`、`/mood`、`/support`、`/help`、`/memories`、`/forget`、`/reset`、`/join`、`/leave`、`/status`）都還在，這裡只列**新的**。

## 新的斜線指令

`/reminders` 你設的提醒ㅤ`/todos` 你的待辦ㅤ`/checkin_status` 主動關心的狀態ㅤ`/voice_wake` 語音喚醒開關

## 前綴指令（上一篇沒列，斜線指令壞掉時的備援）

`!nana`（`!奈奈`）跟她說話ㅤ`!reset`（`!重置`）ㅤ`!help`（`!幫助`）ㅤ`!mood`（`!心情`）ㅤ`!support`（`!支援`）

## 直接講就好的（不用指令）

- **提醒**：「30分鐘後提醒我倒垃圾」「每天晚上10點提醒我吃藥」「取消倒垃圾那個提醒」
- **待辦**：「幫我記一下要買牛奶」「我的待辦有哪些」「買牛奶做完了」
- **主動關心**：「每天晚上9點來關心我一下」「不要主動來找我」「你可以偶爾主動關心我」
- **天氣**：「台北明天會下雨嗎」
- **計算**：「幫我算 1234 乘以 56」

## 管理員指令（上一篇沒列）

`/settings view` 看目前所有設定

**表情**
`/settings reactions` 主動按表情開關ㅤ`/settings reaction_follow` 跟著別人按開關ㅤ`/settings reaction_rate` 頻率節流

**搭話與情緒**
`/settings reply_on_mention` @她時回覆ㅤ`/settings reply_on_keyword` 提到關鍵字時回覆（**目前關閉**）ㅤ`/settings fixed_channel` 專屬對話頻道ㅤ`/settings emotion_support` 即時情緒安慰（**目前關閉**）ㅤ`/settings emotion_threshold` 情緒門檻

**危險訊息**
`/settings alert_channel` 警告頻道ㅤ`/settings disable_alert` 停用警告

**新成員審核**
`/settings review_channel` 審核頻道ㅤ`/settings review_remove_role` 通過後移除的角色ㅤ`/settings review_add_role` 通過後加上的角色ㅤ`/approve @成員` 手動通過

---

<!-- ════════ 第 8 則 ════════ -->

# 🔍 兩件一直在跑、但之前沒寫進說明的事

這兩個**都不是新功能**，是我上一篇漏了沒講，補上。

## 一、危險訊息會通報

她會偵測訊息裡的情緒。如果偵測到**明確的自傷、想結束生命**的訊號，會把那則訊息**通報到管理員的警告頻道**，讓人可以趕快去看看你。

這件事我覺得應該講明白，而不是讓大家以為只是在跟一個機器人聊天。判斷標準設得很嚴 —— 單純發牢騷、難過、抱怨工作、開玩笑都**不會**觸發，只有明確講到想死、自殘、已經傷害自己才會。

（前面提到的「即時情緒安慰」雖然關掉了，**這條通報沒有關**。）

## 二、新成員審核是 AI 在看

審核頻道的表單是奈奈先讀、先判斷，不確定的會追問，太複雜或看起來需要人處理的會轉給管理員。所以你在那邊看到她提問，那是審核流程，不是她在閒聊。

## 🐛 表情相關的問題怎麼回報

回報方式跟上一篇一樣（做了什麼／她回了什麼／大概什麼時間），但表情的問題請特別註明是哪一種：

- **該按沒按** → 我調她的判斷標準
- **不該按亂按** → 我調她的判斷標準（另一個方向）
- **一直刷、太吵** → 我調頻率和間隔

三個處理方式不一樣，講清楚我才知道要動哪裡。

> 如果你或身邊的人正在經歷難關 —— 安心專線 **1925**、生命線 **1995**，24 小時都有人在。
