# 奈奈更新公告（技術版）

給想知道「到底改了什麼」的人。可分則貼上 Discord，每則皆在 2000 字內。
Discord 不支援表格，故全部以清單呈現。

---

<!-- ════════ 第 1 則 ════════ -->

# 🔧 奈奈更新 ── 2026/08/02

這次修了 6 個 bug、加了 4 個功能。先講最扯的：**奈奈其實已經死了 10 天。**

## 💀 為什麼沒人發現奈奈掛了

主機在 7/22 15:04 重開機。log 最後一筆是 `Closing the event loop` —— 那是正常關機，不是 crash，所以奈奈本身沒問題。

問題出在開機之後：`/var/log/supervisor/` 這個目錄不知何時被刪了，導致 supervisord 一啟動就死：

```
Error: The directory named as part of the path
/var/log/supervisor/supervisord.log does not exist
status=2/INVALIDARGUMENT
```

systemd 每 50 秒重試一次，**整整重試了 17,929 次**。而 `autostart=true` 從頭到尾沒生效過。

更容易誤判的是：`ps` 看得到一個活著的 `supervisord`，但那是跑在 docker container 裡的，跟主機無關。

補上目錄後 systemd 當場自己救回來，另外加了 `LogsDirectory=supervisor` 讓它以後自動重建。

順帶一提，同一批躺了 10 天的還有 linebot、f5-tts、minicpm 等共 11 個服務，現在全回來了。

---

<!-- ════════ 第 2 則 ════════ -->

## 🎙️ 語音為什麼叫不動 ── 三個疊在一起的 bug

### 1️⃣ Silero VAD 是有狀態的，但從來沒被重置

語音辨識前要先用 VAD 判斷「有沒有人在講話」。這個模型是 RNN、**有內部狀態**，而整份程式共用同一個實例、從不呼叫 `reset_states()`。

拿同一段錄音實測，speech probability：

- 乾淨狀態 → **0.959**（正常偵測到 6 個語音視窗）
- 灌入 2 分鐘音訊後 → **0.005**（偵測到 0 個，等於全聾）
- `reset_states()` 後 → **0.959**（復原）

也就是說：**第一個人用完，之後所有人都聽不見，要重啟服務才好一次。** 這完全對上 log —— 那晚只有第一通成功回話。

改成每個連線一份獨立實例（實測建立成本 22ms，可忽略）。

### 2️⃣ 只在「講完話」重置還是會鎖死

奈奈在沒人講話時仍會持續送靜音填充，那串靜音一樣會把狀態帶偏 → 偵測不到語音 → 永遠不會有「講完話」→ **永遠不會重置**。死鎖。

加了待機 1.5 秒自動重置。

### 3️⃣ 中文短句被靜默丟棄

系統要求一句話至少 400ms 才送去辨識。但「奈奈」「哈囉」只有 190–350ms，**整段被丟掉而且不留任何 log** —— 從外面看就只是「叫它都不理人」。

跑了參數矩陣，改成 0.35 / 250ms，並補上丟棄時的 log。

---

<!-- ════════ 第 3 則 ════════ -->

## 🔇 為什麼話講一半會斷

修好上面三個之後，換成「講到一半突然閉嘴」：

```
02:21:39  開始播放回覆（5.56 秒的音訊）
02:21:40  🛑 Barge-in: 打斷 AI     ← 才播 1 秒就被清空
02:22:31  回覆內容：「有什么轻松的话题」  ← 半句話
```

原因是**聲學回授**：你喇叭放出奈奈的聲音 → 被你自己的麥克風收回去 → 奈奈以為你要插話，就自己閉嘴了。

有趣的是，語音服務的原始碼裡早就寫了這個警告：

> Barge-in is **disabled by default** because TTS playback **echoing back through the mic** would fire VAD almost immediately.

但設定把它打開了，理由是「前端已過濾回授」—— 那對**瀏覽器**成立（瀏覽器有回音消除 AEC），**Discord 機器人沒有**。加上奈奈這端的門檻只有振幅 500（環境噪音等級）而且單一幀就觸發。

現在改成：AI 播放期間送靜音，要**大聲蓋過去、持續 0.3 秒**才算真的插話。

> ⚠️ 這個門檻是推估值，還沒經過真人校準。如果你發現「想插話卻打不斷」，回報一下我調。

---

<!-- ════════ 第 4 則 ════════ -->

## 🐛 一個我自己寫出來的 bug

加完讀檔功能後，有人回報奈奈把他的名字當成檔案：

> 看到你傳來一個名為 `vito.ipynb` 的檔案…

那位的 Discord 暱稱正是 **`【窮鬼】vito.ipynb`**。

訊息送給模型的格式是 `[暱稱] 說：內容`，而我為了讓奈奈看得懂附件，在系統提示裡常駐加了「對方可能會傳圖片或檔案給你」—— **這句話把模型的注意力導向檔案**，它就把暱稱裡的 `.ipynb` 幻覺成附件了。

修法是把檔案指引改成**只有真的有附件時才注入**，並明確告訴模型方括號裡是暱稱、可能長得像檔名。

## 🧠 記憶：一個容易忽略的坑

新做的長期記憶是 SQLite + 向量檢索。參考現有實作時發現一個 bug 值得說：

原版用 Python 內建的 `hash()` 算向量。但**字串的 `hash()` 每個 process 都是隨機的**：

```
hash("奈奈") =  5697272062376939851
hash("奈奈") = -4969793200959635864
hash("奈奈") =  5976507566505802990
```

向量存進資料庫後，**下次重啟算出來的查詢向量跟舊資料完全對不起來，檢索永久失效**，而且不會報錯，只會安靜地什麼都撈不到。

奈奈這版改用 `blake2b`，已驗證跨 process 檢索正常。

---

<!-- ════════ 第 5 則 ════════ -->

## ✨ 新功能（技術細節）

**📎 附件讀取**
- 圖片轉 base64 走 Gemma 4 的 vision
- PDF 先用 `pypdf` 抽文字；抽不到（掃描檔）就用 `PyMuPDF` 轉圖走 vision
- 中文編碼依序嘗試 utf-8 → big5 → cp950 → gb18030
- **圖片的 base64 不進對話歷史**，只存 `[圖片: x.png]` 佔位 —— 否則每輪重送整包圖，context 很快就爆（後端 `--parallel 4`，每個 slot 實際只有 32k）

**🔎 上網搜尋**
免金鑰。實測後重挑來源 —— Brave 改版成 JS 殼、DDG lite 回 202 挑戰頁、Mojeek 和 searx.be 都擋 captcha，**只剩 DuckDuckGo html 能用**，另補 DDG Instant Answer 與 Wikipedia API 當備援。順手濾掉混在結果裡的 DDG 廣告。

分流是兩段式的：明確說「查一下」直接搜；像在問資訊才呼叫模型判斷；**單純聊天完全不碰網路，也不多花一次 LLM 呼叫**。

**🧠 長期記憶**
抽取放在回覆送出後的背景任務，不影響回話速度。相似度 ≥0.88 視為同一件事改成更新。姓名職業這類基本資料每次都帶上，不靠相似度撈。不記「今天好累」這種會過期的情緒。

**⏳ 階段提示**
單一訊息 in-place edit，回覆前刪除。只在真的有慢動作時出現，純聊天不會多一則訊息。

## 📋 相關指令

`/memories` 看我記得你什麼ㅤ`/forget` 清空記憶ㅤ`/reset` 只重開對話（記憶保留）

> 語音如果之前就在頻道裡，請先 `/leave` 再 `/join`，新設定才會生效。
